#include <stdio.h>
#include <string.h>

int main()
{
    int numberOfTests;

    char gameString;

    int i, j;

    scanf("%d",&numberOfTests);

    for (i = 0; i < numberOfTests;i++){
        printf("digite %d i cano\n", i);
        scanf(" %c %c", &pipeInit, &pipeOut);

        printf("init=%c , out=%c\n", pipeInit, pipeOut);

        //addpipe
        //resolvepipe
    }

    void solveGameString(char[] gameString){
        int previousPosition;
        for (i = 0; i < strlen(gameString); i++)
        {
            if(i>0){
                previousPosition = i - 1
            }

            if 
        }
    }

    return 0;
}